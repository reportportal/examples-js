{
  "scripts": {
    "build": "vite build",
    "test": "vite build && vitest run",
    "test:ui": "vite build && vitest --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@vitest/ui": "latest",
    "playwright": "^1.41.0",
    "vite": "latest",
    "vitest": "latest",
    "@reportportal/agent-js-vitest": "^5.0.0"
  }
}
